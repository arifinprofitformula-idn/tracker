"use client";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { FormEvent, useState } from "react";
export default function AuthForm({mode}:{mode:"login"|"register"}){const router=useRouter();const [busy,setBusy]=useState(false),[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const fd=new FormData(e.currentTarget);const body=Object.fromEntries(fd);const r=await fetch(`/api/auth/${mode}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const data=await r.json();if(!r.ok){setError(data.error||"Terjadi kesalahan");setBusy(false);return}router.replace("/dashboard");router.refresh()}return <main className="auth-shell"><form className="auth-card" onSubmit={submit}><div className="eyebrow">Coach Arifin</div><h1>{mode==="login"?"Selamat datang":"Mulai perjalanan"}</h1><p className="muted">Satu sistem ringan untuk menjaga konsistensi harian.</p>{mode==="register"&&<div className="field"><label>Nama</label><input name="name" required minLength={2} autoComplete="name" /></div>}<div className="field"><label>Email</label><input name="email" type="email" required autoComplete="email" /></div><div className="field"><label>Password</label><input name="password" type="password" required minLength={mode==="register"?10:1} autoComplete={mode==="login"?"current-password":"new-password"}/></div>{error&&<p className="error">{error}</p>}<button className="primary full" disabled={busy}>{busy?"Memproses…":mode==="login"?"Masuk":"Buat akun"}</button><p className="muted" style={{textAlign:"center",fontSize:13}}>{mode==="login"?<>Belum punya akun? <Link className="link" href="/register">Daftar</Link></>:<>Sudah punya akun? <Link className="link" href="/login">Masuk</Link></>}</p></form></main>}
